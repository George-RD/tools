# Gradients

<!-- GENERATED by docsgen/gen_luau_api.py from
     scripting_workspace/src/tools/builtins -- do not edit -->

Declares: `Gradient`, `GradientStop`

```luau
--- Represents a gradient used for filling or stroking a shape. A gradient is
--- defined by a set of color stops (`GradientStop`) and either a linear or radial configuration.
declare extern type Gradient with
end

--- A color stop in a `Gradient`, defined by its position in the range [0, 1] and
--- the color at that position.
export type GradientStop = {

    --- Position of the stop along the gradient, where 0 is the start and 1 is
    --- the end.
    --- ```lua highlight={3,7}
    --- local g = Gradient.linear(Vector.xy(0, 0), Vector.xy(100, 0), {
    ---     {
    ---         position = 0,
    ---         color = Color.rgb(255, 0, 0)
    ---     },
    ---     {
    ---         position = 1,
    ---         color = Color.rgb(0, 0, 255)
    ---     },
    --- })
    --- ```
    position: number,

    --- `Color` at the specified position.
    --- ```lua highlight={4,8}
    --- local g = Gradient.linear(Vector.xy(0, 0), Vector.xy(100, 0), {
    ---     {
    ---         position = 0,
    ---         color = Color.rgb(255, 0, 0)
    ---     },
    ---     {
    ---         position = 1,
    ---         color = Color.rgb(0, 0, 255)
    ---     },
    --- })
    --- ```
    color: number,
}

declare Gradient: {

    --- Creates a linear gradient that transitions between the specified color
    --- stops along the line from 'from' to 'to'.
    ---
    --- ```lua
    --- local g = Gradient.linear(Vector.xy(0, 0), Vector.xy(100, 0), {
    ---   { position = 0, color = Color.rgb(255, 0, 0) },
    ---   { position = 1, color = Color.rgb(0, 0, 255) },
    --- })
    --- ```
    linear: @checked (from: Vector, to: Vector, stops: { GradientStop } ) -> Gradient,

    --- Creates a radial gradient centered at 'from', extending outward to the
    --- given radius, using the specified color stops.
    ---
    --- ```lua
    --- local g = Gradient.radial(Vector.xy(50, 50), 40, {
    ---   { position = 0, color = Color.rgb(255, 0, 0) },
    ---   { position = 1, color = Color.rgb(0, 0, 255) },
    --- })
    --- ```
    radial: @checked (from: Vector, radius: number, stops: { GradientStop } ) -> Gradient,
}
```
